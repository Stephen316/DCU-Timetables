# Build output and CI logs (scripts/ci.sh)
build/

# macOS
.DS_Store

# Misc
*.log

# Personal data — student name→group lists must never be committed or shipped
local-data/
*.local.json

# Local secrets and settings (Supabase keys for the app go in mobile/.env.local)
.env.local
